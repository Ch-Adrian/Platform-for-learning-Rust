version: '3'
services:
  backend:
    build:
      context: .
      dockerfile: ./docker/backend/Dockerfile 
    ports:
      - "8080:8080"
    networks:
      - common_network
  
  frontend:
    build:
      context: .
      dockerfile: ./docker/frontend/Dockerfile 
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
        - common_network

networks:
  common_network:
